# we need flaskv9 to detect objects using the weight
# git clone https://github.com/WongKinYiu/yolov9.git ultralytics/yolov9-main


# flask_modals package last few lines uses Markup without importing it, so we use a modified fork of the package 
#git clone https://github.com/Tareq-Abbas/flask_modals_Markup.git  flask_modals_Markup


# we also need to download the weights of the trained model
